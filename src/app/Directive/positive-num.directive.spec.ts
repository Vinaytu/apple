import { PositiveNumDirective } from './positive-num.directive';

describe('PositiveNumDirective', () => {
  it('should create an instance', () => {
    // const directive = new PositiveNumDirective();
    // expect(directive).toBeTruthy();
  });
});
